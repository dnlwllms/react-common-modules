import styled from "styled-components";

const ChartJSStyledComponents = {
  Container: styled.div``,
  StyledCanvas: styled.canvas`
    width: 100%;
    height: 100%;
  `,
};

export default ChartJSStyledComponents;
