import styled from "styled-components";

const CellectStyledComponents = {
  Container: styled.table`
    user-select: none;
    border-spacing: 0;
    td {
      padding: 0;
      border: none;
    }
  `,
};

export default CellectStyledComponents;
